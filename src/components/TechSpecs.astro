---
const specs = [
  {
    category: 'Supported Formats',
    items: ['OpenAPI 3.0 & 3.1', 'Swagger 2.0', 'Postman Collections', 'AsyncAPI', 'GraphQL Schemas']
  },
  {
    category: 'Export Options',
    items: ['Static HTML', 'React Components', 'Markdown (MDX)', 'PDF Document', 'JSON/YAML']
  },
  {
    category: 'Integrations',
    items: ['GitHub Actions', 'GitLab CI', 'Bitbucket Pipelines', 'VS Code Extension', 'Slack Alerts']
  },
  {
    category: 'Deployment',
    items: ['Custom Domains', 'AWS S3', 'Vercel / Netlify', 'Docker Containers', 'On-Premise']
  }
];
---

<section class="section bg-white text-gray-900 transition-colors duration-300 dark:bg-gray-950 dark:text-gray-100">
  <div class="container-custom">
    <div class="grid md:grid-cols-2 gap-16 items-center">
      <!-- Left Content -->
      <div>
        <h2 class="text-3xl md:text-4xl font-display font-bold mb-6">
          Built for Your <span class="text-transparent bg-clip-text bg-gradient-to-r from-blue-600 to-purple-600 dark:from-blue-400 dark:to-purple-400">Tech Stack</span>
        </h2>
        <p class="text-gray-600 text-lg mb-8 leading-relaxed dark:text-gray-400">
          Whether you're using modern OpenAPI 3.1 or maintaining legacy Swagger files, LampLight adapts to your workflow. Integrate directly with your CI/CD pipeline and keep docs in sync with code.
        </p>
        
        <div class="flex flex-wrap gap-3">
          <span class="px-4 py-2 rounded-full bg-gray-100 text-gray-700 text-sm font-mono border border-gray-200 hover:bg-gray-200 transition-colors dark:bg-gray-800 dark:text-gray-200 dark:border-gray-700 dark:hover:bg-gray-700">npm install @lamplight/cli</span>
          <span class="px-4 py-2 rounded-full bg-gray-100 text-gray-700 text-sm font-mono border border-gray-200 hover:bg-gray-200 transition-colors dark:bg-gray-800 dark:text-gray-200 dark:border-gray-700 dark:hover:bg-gray-700">docker pull lamplight/core</span>
        </div>
      </div>

      <!-- Right Grid -->
      <div class="grid grid-cols-2 gap-6">
        {specs.map((spec) => (
          <div class="bg-white p-6 rounded-xl border border-gray-200 hover:bg-gray-50 transition-colors dark:bg-gray-900 dark:border-gray-800 dark:hover:bg-gray-800">
            <h3 class="font-bold text-lg mb-4 text-gray-900 dark:text-gray-100">{spec.category}</h3>
            <ul class="space-y-2">
              {spec.items.map((item) => (
                <li class="flex items-center text-gray-600 text-sm dark:text-gray-300">
                  <svg class="w-4 h-4 mr-2 text-green-600 dark:text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                  </svg>
                  {item}
                </li>
              ))}
            </ul>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>
